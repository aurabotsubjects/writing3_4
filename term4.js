/* ============================================================
   Writing Warm-Up — Term 4 (Weeks 1–10)
   Requires core.js to be loaded first (defines g() and ctx()).
   Not yet built — see curriculum map for planned content.
   ============================================================ */

const TERM4={};
